题目：在表t1中存储如下内容，列2中的值用固定分隔符”，“进行分割:
c1              c2
1               a,b
2               c,d,e
3               f,g
请编写SQL，运行并得到如下结果：
c1          c3
1            a
1            b
2            c
2            d
2            e
3            f
3            g

8.0测试
```
算出字段
mysql> select c1,c2,length(c2)-length(regexp_replace(c2,'[a-g]','')) from t1;
+------+-------+--------------------------------------------------+
| c1   | c2    | length(c2)-length(regexp_replace(c2,'[a-g]','')) |
+------+-------+--------------------------------------------------+
|    1 | a,b   |                                                2 |
|    2 | c,d,e |                                                3 |
|    3 | f,g   |                                                2 |
+------+-------+--------------------------------------------------+

mysql> SELECT SUBSTRING_INDEX(SUBSTRING_INDEX('a,b,c',',',1),',',-1);
+--------------------------------------------------------+
| SUBSTRING_INDEX(SUBSTRING_INDEX('a,b,c',',',1),',',-1) |
+--------------------------------------------------------+
| a                                                      |
+--------------------------------------------------------+
1 row in set (0.00 sec)

mysql> SELECT SUBSTRING_INDEX(SUBSTRING_INDEX('a,b,c',',',2),',',-1);
+--------------------------------------------------------+
| SUBSTRING_INDEX(SUBSTRING_INDEX('a,b,c',',',2),',',-1) |
+--------------------------------------------------------+
| b                                                      |
+--------------------------------------------------------+
1 row in set (0.00 sec)

mysql> SELECT SUBSTRING_INDEX(SUBSTRING_INDEX('a,b,c',',',3),',',-1);
+--------------------------------------------------------+
| SUBSTRING_INDEX(SUBSTRING_INDEX('a,b,c',',',3),',',-1) |
+--------------------------------------------------------+
| c                                                      |
+--------------------------------------------------------+
1 row in set (0.00 sec)

mysql> select * from t1,(select 2 union all select 2 ) t where t1.c1=3;
+------+------+---+
| c1   | c2   | 2 |
+------+------+---+
|    3 | f,g  | 2 |
|    3 | f,g  | 2 |
+------+------+---+

```